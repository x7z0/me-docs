# 词库结构 

> 以最简单的词库为例介绍词库的结构

<details>
<summary>hdic.txt (点击展开)</summary>

```js 
[词库信息]
//从这里开始，如果你要注释信息请在行头加入双斜杠//
//这里将是您对这个词库的标注，请注意使用英文的 : 
名称:新词库参考用模板
版本:1.0
作者:忘忧草
简介:这只是一个演示dic，请勿在未知的情况下随意使用
//以下两个是必须设置的，用于每个群单独开启关闭机器人指令，使用前请先设置主人
开机:开启AI
关机:关闭AI

[常量]
//这里将是对于不擅长记忆英文单词一部分人所准备的，为了缩小词库体积和制作自己习惯的编写方式，允许对一部分的内容进行短语替换，使用常量请加上#号"

replaceAll>>全部替换
calc>>计算
Api->sendMsg>>发送
ToolKit->randomText>>随机文本
{"QQ":"@uin","群号":"@group","昵称":"@nick","变量":"@a"}>>信息

[模块]
//此处是模块区域。模块名不支持正则匹配
//下面两个对应[词库信息]中的开关机，开机指令更改则此处也需要更改成一样的

开启AI
本群AI已开启

关闭AI
本群AI已关闭

[词条]
菜单
我是菜单
功能1 功能2

```

</details>

## [词库信息]
> 写在词库头部的信息配置

|字段|例子|说明|
|---|---|---|
|名称|示例词库
|版本|1.0|
|作者|忘忧草|
|简介|这只是一个演示dic|
|开机|开启AI|必填，作为开机指令
|关机|关闭AI|必填，作为关机指令

## [常量]

> 常量是以“常量值>>常量名”形式定义的，在调用时用“#常量名”代替“常量值”<br>
> 只是快捷式的文本代替，未限制常量值是否有意义

如果常量定义如下：
```js
replaceAll>>全部替换
calc>>计算
Api->sendMsg>>发送
ToolKit->randomText>>随机文本
10001>>主人QQ
{"QQ":"@uin","群号":"@group","昵称":"@nick","变量":"@a"}>>信息
```

那么，在词条和模块中有以下等同关系：

|带常量|不带常量|
|---|---|
|ToolKit->#全部替换()|ToolKit->replaceAll()|
|#计算(9-6)|calc(9-6)|
|#发送()|Api->sendMsg()|
|#随机文本()|ToolKit->randomText()|
|#主人QQ|10001|



## [模块]

> 模块相当于预先定义的子函数，便于复用，只是无传参，包含内部模块和自定义的模块<br>
> 在调用时用 **Mod(模块名)** 即可调用<br>
> 模块执行后，流出的有效内容会被视为返回值

目前的内部模块仅有开机和关机模块，且模块名（指令）由开发者自定义，开关机模块可以直接发送回复消息（等同于词条）

```
[模块]
打卡
巴拉巴拉打卡功能

打卡天数
20

[词条]
早上好
Mod(打卡)
早上好喵，已经帮你打卡了~

查询
a:Mod(打卡天数)
你打卡了 @a 天~
```

## [词条]

> 词条就是你最期待的部分，负责具体的指令和响应，首行写你想匹配的消息，支持正则匹配<br>
> 可以直接在第二行开始写文本内容，快速发送消息

写法：

```
你好
你好喵~

测试
Api->sendMsg(测试成功)

我叫(.*)
好的，你叫 @k1 ,我记住了~
```